#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
int main(int argc, char *argv)
{
	int a,b;
	if(argc!=3)
	printf("parametes mismatched/////");
	else
	{
		a=atoi(argv[1]);
		b=atoi(argv[2]);
		printf("%d\t%d",a,b);
	}

}